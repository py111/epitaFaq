{% extends '::base.html.twig' %}

{% block body -%}
    <h1>questionanswer</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ entity.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ entity.answer }}</td>
            </tr>
            <tr>
                <th>Resource</th>
                <td> </td>
            </tr>
        </tbody>
    </table>

            <a class="record_actions"> <br>
    
        <a class="btn btn-default btn-xs" href="{{ path('questionanswer') }}">
            Back to the list
        </a> <br>
    
        <a class="btn btn-default btn-xs" href="{{ path('questionanswer_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    {{ form(delete_form) }}
</a>
{% endblock %}
