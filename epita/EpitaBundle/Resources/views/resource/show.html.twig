{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Resource</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Resource</th>
                <td>{{ entity.resource }}</td>
            </tr>
        </tbody>
    </table>

            <a class="record_actions"><br>

        <a class="btn btn-default btn-xs" href="{{ path('resource') }}">
            Back to the list
        </a><br>

        <a class="btn btn-default btn-xs" href="{{ path('resource_edit', { 'id': entity.id }) }}">
            Edit
        </a>
        {{ form(delete_form) }}
    </a>
{% endblock %}
