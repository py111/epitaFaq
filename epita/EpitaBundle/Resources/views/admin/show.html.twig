{% extends '::base.html.twig' %}

{% block body -%}
    <h1>admin</h1>

    <table class="record_properties table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ entity.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ entity.password }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ entity.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ entity.lastname }}</td>
            </tr>
        </tbody>
    </table>

            <a class="record_actions"> </a>
    
        <a class="btn btn-default btn-xs" href="{{ path('admin') }}">
            Back to the list
        </a>
            <br>
    
        <a class="btn btn-default btn-xs" href="{{ path('admin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    {{ form(delete_form) }}

{% endblock %}
